# Installation

1. Create Python Venv: Use a shell of your choice and navigate to this project directory. Run

`python -m venv venv`


2. Install all requirements

`pip install -r requirements.txt`


3. Use PyCharm to launch the application (play button)
