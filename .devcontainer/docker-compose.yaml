services:
  devcontainer_asset_connector:
    build: .
    container_name: python_container_ac
    volumes:
      - ../..:/workspaces:cached
    command: sleep infinity
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  bridge:


